#pragma once
#include "types.h"

/* triangulate the partition using Algorithm 2.4. It is assumed that a list of 
 * points is in the workspace, but no tri's yet. We will create them. */
void triangulate( workspace *w);
